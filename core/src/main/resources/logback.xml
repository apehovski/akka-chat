<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.out</target>
        <encoder>
            <pattern>%-5level %d{"yyyy-MM-dd'T'HH:mm:ss.SSS"}Z %logger{0} - %replace(%msg){'[\r\n]+', '\\n'} [%X{akkaSource}] [%thread]%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/app.log</file>
        <append>false</append>
        <encoder>
            <pattern>%date{yyyy-MM-dd} %X{akkaTimestamp} %-5level[%thread] %logger{1} - %msg [%X{akkaSource}]%n</pattern>
        </encoder>
    </appender>

    <logger name="akka" level="INFO"/>
<!--    <logger name="org.asynchttpclient.netty" level="INFO"/>-->

    <root level="DEBUG">
        <appender-ref ref="CONSOLE"/>
        <!--<appender-ref ref="FILE"/>-->
    </root>

</configuration>


