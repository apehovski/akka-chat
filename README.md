# akka-chat

Pet project for some learning in scope of modern web-technologies

## Run locally
In directory /core:  
`sbt run`

## Run on Docker
In directory /core:  
`sbt stage`  

In directory /docker:  
`docker-compose up`  
